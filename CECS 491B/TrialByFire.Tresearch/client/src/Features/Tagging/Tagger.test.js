import { render, screen } from '@testing-library/react';
import Tagger from './Tagger';

test('renders with navigation bar', () => {
    // Arrange
    let nodes = [2];
    let tagName = 'art'

    // Act
    let result = Tagger.GetTagData();

    // Assert
})